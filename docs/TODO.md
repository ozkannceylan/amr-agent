# TODO

## interface
- m1-01 VDA 5050 subset — done when every message/field in docs/interfaces/vda5050-subset.md traces to the official VDA 5050 v2 schema.
- m1-02 OPC UA node model — done when docs/interfaces/opcua-nodes.md lists every node with PLC-mirrored PascalCase names, types and access.
- m1-03 handshake tables — done when docs/interfaces/handshake-tables.md covers conveyor/door/charger handshakes and a single-owner map for every shared data item.

## verifier
- m1-04 review M1 — done when a pass verdict is filed in docs/reports/.
