# LESSONS

Append only. One entry per correction, dead end or surprise.
Format: date | what was attempted | what went wrong | the rule now

2026-07-26 | Planned M0 briefs against the section 5 roster | No roster agent has write access to the repo root, docs/adr/ or docs/roadmap.md, so bootstrap work had no owner | Infra deliverables go to an ad-hoc infra agent, approved by the owner per brief list
2026-07-26 | Session provisioned with branch claude/m0-gate-repo-skeleton-nu4br4 | Conflicted with the CLAUDE.md branch template | The CLAUDE.md template wins; owner confirmed docs/infra-repo-skeleton; surface the conflict instead of silently picking a branch
2026-07-26 | Committed M0 work with the environment's default git identity | Verifier found tooling author metadata in git log even though commit messages were clean | Author fields count as attribution; set repo-local user.name and user.email to the owner before the first commit of every session
2026-07-26 | Owner briefs extended the interface agent's write scope to docs/adr/ and docs/roadmap.md per brief | Per-brief scope exceptions are recurring noise; interface contracts and architecture records are different jobs | New arch-docs agent owns docs/adr/, docs/roadmap.md and docs/PLAN.md; the interface agent stays inside docs/interfaces/
2026-07-26 | ADR 0002 cited live vendor repos as evidence | Claims like "jazzy-devel is the default branch" will silently age with no pinned ref to re-check against | When an ADR cites external vendor sources, record the verification date and, where possible, a pinned ref or commit so the claim stays re-checkable
2026-07-27 | The bridge was designed into fleet/bridge/ | fleet/README.md forbids ROS 2 access, correctly for the fleet manager, so the bridge would have needed an exception written into another layer's boundary statement | A component that cannot live inside a layer without weakening that layer's boundary is its own layer; ADR 0005 made bridge/ top level
2026-07-27 | asyncua was installed system-wide | pip 24.0 refuses to replace Debian's cryptography, so the install failed | Install the bridge's Python dependencies into /opt/amr-bridge-venv created with --system-site-packages so rclpy still imports
2026-07-27 | A ROS 2 subscriber callback was named _clock in an rclpy node | rclpy's Node already owns self._clock, so the callback was silently shadowed and surfaced as a misleading "callback should be callable with one argument" error | Name ROS 2 callbacks cb_* in this project
2026-07-27 | m3-02 (node model) was delegated in parallel with m3-01 (sim signal table) | The node set had to be designed blind and then reconciled, costing an extra brief | Delegate a contract document only after the document it describes exists, or accept a reconciliation brief as part of the plan
2026-07-27 | Gates were sequenced vehicle-first, so the project's core claim (the Gazebo-to-PLC loop) sat behind three gates of mobile robot work | An owner scope correction stopped M3 mid-run; a Nav2 scenario was left unverified and had to be parked | Sequence gates so the core architectural claim is proven first; when work is parked, mark it unverified in place rather than deleting or leaving it silently
2026-07-27 | A subagent ended its turn with detached wait loops "armed", expecting to be re-invoked | Detached processes do not notify; the agent stopped without finishing its verification run | Agents drive their own runs to completion with bounded foreground polling; never end a turn waiting on a detached process
2026-07-26 | m1-02 was revised to satisfy m1-03's "additions required" list | The requesting document (handshake tables §6/§7) was not updated in the same commit, so two same-gate docs contradicted each other and the verifier failed the gate | When a revision resolves another document's request, update the requesting document in the same commit
2026-07-26 | ADR 0003 closed the distro decision and TODO was updated | PLAN.md kept the stale "decision queued" line, so PLAN and TODO disagreed until the verifier caught it | When a report closes an item, update PLAN.md in the same commit as TODO.md, not later
2026-07-27 | The m3-03c brief enumerated the stale-path locations as §1/§9.4/§10/§12 | The real count was seven occurrences in six locations; §11's install path was missed, and one §1 clause needed rewording rather than a path swap because its "even though" only existed to excuse the provisional location | A brief that enumerates occurrences states the list as a starting point and instructs the agent to verify it by independent search; never let an enumerated list read as exhaustive
2026-07-27 | Container-side M3 evidence was treated as sufficient to describe the loop as proven | The owner's machine is WSL, where Gazebo was absent and no venv existed, so none of the evidence had ever been reproduced on the platform the demonstration will run on | Evidence is qualified by the environment that produced it; re-verify on the target platform before a gate advances and keep both evidence sets rather than replacing one with the other
2026-07-27 | Ran sim/setup/install.sh in WSL from the /mnt/c checkout | Windows Git's system autocrlf = true had checked the tree out as CRLF, so the shebang failed with /usr/bin/env: 'bash\r' and every tracked file read as modified to WSL-side git | The repo needs a root .gitattributes (* text=auto, *.sh text eol=lf); until it exists, run git from Windows only and pipe shell scripts through sed 's/\r$//' before executing them in WSL
2026-07-27 | Assumed WSL2's NAT'd adapter would need DDS tuning, as is generally reported | Default discovery worked in all four tested configurations; the tuning would have been cargo-cult configuration masking nothing | Measure discovery on the actual machine before adding ROS_LOCALHOST_ONLY or an XML profile; note ROS_LOCALHOST_ONLY is deprecated in Jazzy and logs a warning
2026-07-27 | Benchmarked DrvFs with a per-row flushed CSV and nearly reported a 154x penalty as a blocker | The bridge does not write that way; instrumentation.py batches rows and flushes every 2 s, making the real cost ~0.2 % of wall time | Benchmark the access pattern the code actually uses, not a plausible-looking proxy; read the implementation before quantifying its cost
2026-07-27 | Treated a wall-clock anomaly as a WSL quirk to note in passing | The host's Windows Time service was stopped, making the guest clock step ~2.73 s every 30 s; had the bridge used time.time() this would have silently corrupted every latency figure | Before trusting timestamp-derived evidence on a new machine, verify which clock the code samples and that host and guest clocks are synchronised
2026-07-27 | Reported cell.sdf failing to parse as a possible CRLF casualty | It was actually -- inside an XML comment, which the spec forbids and ElementTree enforces; CRLF was irrelevant | Confirm the mechanism of a failure with a minimal reproduction before attributing it to the environment under investigation
2026-07-27 | Specified a monitored, edge-triggered reset for the demonstration cell | The cell's panel has Start, Stop and process stop and no reset device, and no reset node exists in opcua-nodes.md §9.3, so the domain rule had no device to run on | A gate that mandates a CLAUDE.md §9 behaviour must have the device for it in the signal table; when it does not, implement the behaviour on an existing device with the conflation written out explicitly, and request the missing device rather than inventing a tag
2026-07-27 | Applied "actuator outputs are formed from the cycle-running flag combined with interlocks" to a Real setpoint | The rule reads as a coil-and-contact rule, and the natural translation, a conditional write with no ELSE, leaves the Real holding its last value so the belt runs on after the stop | Gating an analogue output means driving it to zero in a mandatory ELSE branch, assigned unconditionally in exactly one statement; a conditional write is not a gate
2026-07-27 | Wrote the reset condition as "latches clear when the run permissive is satisfied" | The run permissive contains the latches, so each latch became its own precondition for clearing and no reset could ever fire | Split the interlock set in two: a live-world set that a reset tests, and a permissive set that adds the latches and gates running. A latch is never a term in its own clearing condition
2026-07-27 | Read an IEC TON's elapsed time at the falling edge of the button it was timing | A TON returns ET to 0 in the same call in which IN goes false, so a reset that acts on release always measured 0 and never fired | Latch the "held long enough" verdict while the contact is still held; re-arm it on the next rising edge
2026-07-27 | Made "belt inside its soft travel limits" a run permissive | A belt sitting on the limit could never move off it: returning requires running, and running was blocked by the limit | A limit that can only be escaped by moving is a step-level abort in the offending direction, not a blanket permissive; recovery is a re-home branch chosen by re-reading the position at start
2026-07-27 | Wrote a NaN guard for ProductSensorRange as range < threshold | A NaN makes every comparison false, so "no product" is returned, the permissive direction, for a broken sensor | Test an analogue input for plausibility against its physical window before using it in any process comparison, and treat outside-window as a fault rather than as a value
2026-07-27 | Diagnosed the WSL 'bash\r' failure as stored CRLF in the repo | The index was LF-clean all along; only Windows' system-level core.autocrlf=true smudged the checkout, so no file content was ever wrong | Read git ls-files --eol before treating a line-ending failure as a content problem: i/lf w/crlf means fix the checkout policy, i/crlf means fix the content
2026-07-27 | Assumed * text=auto would be enough to make shell scripts executable from WSL | text=auto only normalizes the index; with core.autocrlf=true the working tree is still smudged to CRLF, so the shebang stays broken | Any file executed through a shebang needs explicit eol=lf, not just text=auto; in this repo that is *.sh and the seven *.py files that begin with #!
2026-07-27 | Relied on git's text/binary heuristic for the Nav2 map | The heuristic only scans the first 8000 bytes for a NUL, and map.pgm is regenerated by make_map.py, so a map with a uniform top band would silently reclassify as text and be corrupted by normalization | Mark generated binaries -text explicitly rather than trusting detection, because the cost of a misdetected binary is corruption while the cost of the rule is one line
2026-07-27 | Compared git status between Windows and WSL and found a file listed on one side only | .claude/settings.local.json is covered by the owner's Windows global ignore, which WSL git cannot resolve because HOME differs | Ignore rules that must hold for both sides of a /mnt/c checkout belong in the repo's .gitignore, never in a per-user global ignore file
2026-07-27 | The orchestrator staged explicit paths, then ran a bare git commit while agents worked in parallel | A bare commit writes the whole index, so an agent's staged .gitignore was swept into a commit whose message described unrelated interface work, breaking one-logical-change-per-commit | Under concurrency the orchestrator commits with a pathspec, git commit <paths> -m, never a bare git commit; "leave it staged, the orchestrator commits" is only safe when nothing else is committing
2026-07-27 | Verified a line-ending fix by requiring git status to be empty on both platforms | Concurrent agents kept the tree dirty, so emptiness was never achievable and the file set changed between consecutive commands | The test for a line-ending policy is that Windows and WSL report the same set, not an empty one; confirm each modification is real content by checking git diff --numstat equals git diff --ignore-cr-at-eol --numstat
2026-07-27 | Wrote a section-symbol into .gitignore as UTF-8 | The bytes were valid, but Windows PowerShell 5.1's Get-Content defaults to the ANSI codepage and renders them as mojibake, which is how the owner would read the file | Keep dotfile config comments ASCII-only; a file parsed by two git builds on two platforms should not carry a non-ASCII byte for a decorative character
2026-07-27 | Assumed WSLg's /dev/dri meant Gazebo would render on the GPU | OGRE binds /dev/dri/card0 via EGL and creates a GL 4.5 context, which looks like acceleration, but Mesa falls back to kms_swrast and GL_RENDERER is llvmpipe in every configuration tested | Read GL_RENDERER from the ogre2 log before claiming hardware acceleration; the presence of a DRI node proves nothing, and LIBGL_ALWAYS_SOFTWARE=1 with DISPLAY unset segfaults Gazebo rather than helping
2026-07-27 | Isolated a Gazebo probe from a concurrent agent using ROS_DOMAIN_ID alone | gz transport does not use DDS, so the ROS variable does not isolate the simulation; GZ_PARTITION is the one that does | Isolate both transports when two agents may run Gazebo at once, and match pkill patterns against observed pgrep -af output because gz sim -s -r and gz sim -r -s are the same run with different argument order
2026-07-27 | Mapped a scenario's derived PLr straight onto the PL target of the SF named in its title | The reset scenario derives PLr d while the SRS targets PL c for SF-08, and the mismatch looks like an error until you notice the hazard is actually held by the still-inhibited SF-07 | A PLr belongs to the hazard, not to the function named in the scenario title; state which function actually holds the hazard, and target the named function only against its own credible failure
2026-07-27 | Derived the F parameter for a fault-injection scenario from how often the fault occurs | F in the ISO 13849 risk graph is the person's exposure to the hazard zone, not the fault rate, the demand rate or the duty cycle; deriving it from the fault understates the risk | In any fault scenario, inherit S, F and P from the demand scenario the fault disables; only the required architecture changes, never the PLr
2026-07-27 | Treated a PLr that came out above the SRS target as a document inconsistency to reconcile | It was the opposite in one direction and a real finding in the other: a PLr c under a PL d target is correct because PLr is a floor, while a PLr b under a function with no PL claim is a genuine gap | When a derived PLr and an implemented PL disagree, say which is the floor and which is the build, and never close the gap by re-arguing a parameter
2026-07-27 | Tracking files were updated per report while five agents closed work in one afternoon | The verifier failed check 26 on them anyway: an "in progress" line survived its own closure and PLAN never learned of three briefs, because each update captured the report in hand rather than the whole queue state | When reports close in bursts, reconcile TODO and PLAN against the full report directory before the verifier runs, not against the last report read
2026-07-27 | Swept sim/ for the stale phrase "hold time" with a plain grep | A third occurrence in cell.sdf was wrapped across a line break as "hold" / "time", so the grep reported the file clean and only a whitespace-normalised search caught it | Grep for prose in wrapped documents by normalising whitespace first; a line break sits inside any phrase long enough to be worth searching for
2026-07-27 | The node model specified the OPC UA namespace URI as urn:amr-agent:cell:plc and the PLC spec instructed setting it in TIA Portal | TIA Portal derives a server interface's namespace URI as http://<interface name> and the field is not editable, so the URN could never exist on a real S7-1500 server interface; found at commissioning phase 0 | Spec values authored without the tool that realises them are design values, not facts; the namespace URI joined that list and was corrected at commissioning, and every tool-derived identifier in a spec must be marked as owner-verified-in-tool before a gate relies on it
2026-07-27 | The namespace URN was corrected as a string replacement across four documents | The last stale statement was not the string but a sentence depending on its scope, that two interfaces share one URI, and it only surfaced because ADR 0006 voided it by name | When a tool-derived value is corrected, sweep for statements that depend on its scope, not only occurrences of the string; and where a value is derived, the spec instructs the input that produces it and asks for the derived value to be read back, never entry into a non-editable field
2026-07-27 | The TIA server interface was treated as freely renamable | The interface name IS the namespace URI under ADR 0006, so renaming it silently breaks every browse-by-URI at connect | An OPC UA server interface name carries the same change discipline as a BrowseName, and the M6 fleet-facing interface name is a contract decision to be taken at briefing, not discovered in the tool
2026-07-27 | Phase-0 connectivity was debugged as a single fault guided by TIA's error dialogs | Two independent faults masked each other: the PLCSIM V3 install was broken (no virtual adapter service) and the home LAN collided with the instance subnet (WiFi and PLC both 192.168.0.x, so the .1 ping was answered by the router at TTL=64); neither showed in the dialogs | Diagnose connectivity with ping, TTL and ipconfig before trusting tool error dialogs, and treat a plausible first fault as unconfirmed until the fix changes the symptom
2026-07-27 | TIA "Change device" was used to move the project onto the new CPU | It silently deleted the OPC UA server interface and reset the security settings | After any Change device, re-verify the server interface, access control and runtime license, then re-download
2026-07-27 | A PLCSIM Advanced instance was reconfigured under its existing name | PLCSIM remembers instance names with their old IP configuration and locks the fields | A fresh instance name is the reliable reset for PLCSIM network configuration
2026-07-27 | TCP/IP downloads failed while Softbus downloads worked, and TIA was suspected | Softbus bypasses the IP stack, so Softbus-works-TCP/IP-fails means routing, not TIA | When Softbus downloads work and TCP/IP fails, check for subnet overlap between host adapters and the PLCSIM instance subnet first
2026-07-27 | The m3-21 report and PLAN quoted "22/22 checks" for the connect-conformance harness | The harness prints no count; the verifier reproduced the run and found 18 checks from 19 call sites, so the figure was agent arithmetic, not tool output | Quote counts and measurements only as the harness prints them; a number that appears in no tool output does not go into a report or a tracking file
2026-07-27 | plc-program-spec section 7 called the step dwell TON with IN := TRUE and never released it | Q stays set from the first cycle onward, so every later cycle skips the dwell entirely; found by the owner when the program ran on PLCSIM | A TON owned by a step is released on step exit; a timer that is never released is correct exactly once, and a step timer's IN is the step's own activity, never a literal TRUE
2026-07-27 | The dwell-timer defect was diagnosed as a bad IN expression, to be fixed by releasing the timer on step exit | The call itself sat inside the CASE branch for step 20, a site that stops executing the moment the step is left, so no IN expression placed there could release the timer after exit — only a release executing in the exit scan itself would work | A timer that must be released by leaving a state is called unconditionally outside the CASE, with IN as the state test (IN := (Step = N)); a timer called inside its own branch can only ever be released by code that runs in the same scan as the exit
2026-07-27 | The analogue plausibility rule was applied to ProductSensorRange, the input the earlier NaN lesson named | ConveyorBeltPosition and ConveyorBeltSpeed were left with no plausibility window at all, so a NaN position silently disarms both soft-limit aborts — the same failure the rule exists to prevent, one input over | When a rule is derived from one signal, sweep every signal of the same kind in the document before closing the item; a lesson applied only to the input that taught it is half a fix
2026-07-27 | The implementation omitted the spec's IS_VALID call from the ProductSensorRange plausibility check, on the ground that NaN comparisons all return false | Sound here only because validity is written as an affirmative AND of two comparisons, so NaN falls through to the fault branch; the same omission under a negated out-of-window test would make NaN read as plausible and pass a broken sensor as a value | Write analogue plausibility affirmatively as valid := (low < x) AND (x < high) with the fault in the ELSE, and only then is an explicit NaN test redundant
2026-07-28 | The m3-26 brief told the agent to expect a 10.000 s keep-alive, generalising the one observed grant (3 600 000 ms request cut to 30 000 ms) into "the CPU grants 30 000 ms" | The CPU honoured the bridge's 10 000 ms request unchanged and the keep-alive came out 3.333 s; the 30 000 ms was a cap, not a constant | State an expectation as the rule, granted = min(request, cap), never as the single value one observation produced; the agent caught it only because the design says read the grant back
2026-07-28 | §6.6's case-D detection was specified from a signal-loss capture showing cmd 0.05 with speed 3.2e-28 | That capture was a belt parked on its mechanical stop, so the spec generalised a degenerate state: the one-shot PositionRef window can only catch a freeze in the first sampling window, and a frozen non-zero speed blinds the zero-speed test — the live run went 26.3 s undetected | A stale-data detection promised for "any time during motion" needs a reference that re-arms for as long as motion lasts, and the capture a detection is derived from must be shown to represent the failure mid-motion, not only at rest
2026-07-28 | Section B declared "Pass: all twelve" for the T-scenario run | T4.11 was added to §11 by m3-27 after the live run and appears in neither the as-run record nor the owner-outstanding list, so the pass count silently absorbed a test that never ran | A pass count is derived from the as-run list at writing time, never from the current scenario table; when a spec revision adds a test after a run, the evidence gains an outstanding row, not a bigger denominator
2026-07-28 | F1 was hunted in logic while the interface default read T#100ms | The value in force was T#1M_40S = 100 s, visible only in the watch table: an interface Default value governs nothing once the instance DB exists, and a download without reinitialisation preserves the DB's stale PT forever | Diagnose timer defects from the watch table's in-force values, never from interface defaults; make every TON's PT explicit at the call site so no stale instance value can rule, and after any timer fix verify the in-force value online before re-testing
2026-07-28 | The owner downloaded to the CPU while the bridge held a live session | The CPU's OPC UA server dropped the session mid-read and the bridge died on an unhandled exception in the in-flight request instead of entering its reconnect path, silently ending evidence capture | The reconnect path must catch exceptions thrown by in-flight requests, not only failed connects; until that lands, restart the bridge after every download and check pgrep before trusting a quiet log
2026-07-28 | BridgeLinkOk was implemented as NOT HeartbeatStaleTimer.Q | The verdict boots TRUE for the whole stale window, so ResetDeviceFault's "seen open with the link up" clears on the first scan and a reset held across a restart cleared latches the moment the link formed — T4.9b failed as specified | A link verdict is FALSE until the heartbeat has been seen to change at least once; "not yet proven stale" is not "alive", and every guard that rides on link-up inherits the boot polarity
2026-07-28 | A CPU warm restart happened under a surviving bridge session | The restart reverted every input to its start value and the bridge, which writes on change, never repaired the ones whose slot values had not changed — the PLC read open stop circuits for minutes and the monitored reset correctly refused to clear them | The bridge must detect a server restart (heartbeat node reverting, or session/subscription loss) and rewrite every slot; until then, force-republish every level with a toggle after any CPU restart
2026-07-28 | T4.11 narrowed the speed window to make real values implausible | Zeroing the setpoint recovers the plant in ~100 ms, under the 200 ms fault delay, so the latch can never form; five "silent" start presses were in fact five correct C5 interventions visible only in the 20 Hz CSV | A latch test must hold the offending condition longer than the delay independently of the reaction it triggers; that needs fault injection at the source, not a narrowed window
2026-07-28 | A stopped diagnostics log was read as live PLC state, twice in one day | The process was alive both times, so the pgrep check passed while the log's last line was minutes old, and stale "link TRUE, no latches" lines masked a dead session and then a live latch set | A polled log line is evidence only together with its age; check the process AND that the last timestamp is younger than the poll period
2026-07-28 | "indirdim" was taken as "the CPU runs the new build", twice | One download left project and CPU inconsistent: silent start refusals, watch-table monitoring-error icons on exactly the rows whose DB offsets moved, and an in-force PT that contradicted the code | After every download check the block diff circles are solid green before testing; the monitoring-error icon and an in-force timer PT that contradicts the call site are the live tells of a stale build
2026-07-28 | One dated evidence CSV was reused across seven bridge restarts | --evidence-csv truncates at every bridge start, so each restart silently wiped the day's earlier 20 Hz data; the 4.6 freeze-to-fault measurement had to be re-run | One evidence CSV per bridge session, unique name per start; never share a CSV path across restarts, and salvage the 1 Hz logs beside the CSVs
2026-07-28 | A session's evidence CSV was gzipped for committing while the bridge was still running and still writing it | gzip replaced the file under the live writer, which then recreated it headerless and kept appending for three idle hours to a 400 MB file; the .gz and the leftover are contiguous halves of one stream, not copies, so a prefix comparison between them fails and looks like corruption until the first rows are read | Stop the writer before archiving its file; when a live file must be archived, copy first and compress the copy, and record that the archive is a snapshot of an open file
2026-07-28 | The M3 gate brief was written to rule on plc/demo-cell/SPEC.md §11's step list, the procedure the project had built for itself | §11 had grown stricter than the criterion it demonstrates — it gained steps after the run and split others — so a step list would have kept the gate open on tests the criterion never asked for, while a criterion-only reading would have missed that item (a) names its own instrument | Rule a gate against the roadmap's criterion text, and say per item whether the procedure is stricter, equal or weaker; a procedure step that never ran does not automatically unmeet an item, and a step that passed does not automatically meet one
2026-07-28 | The §6.8 rebuild was re-tested and 4.9b was re-run in its boot form only | The boot form no longer reproduces the failure, but m3-34 had predicted the defect would relocate to bridge restart — where the reset image freezes FALSE and the first attributable TRUE is a genuine edge; only the bridge-restart form actually exercises the per-link-session guard | When a fix relocates a failure mode, the re-run tests the RELOCATED form, not the original; a guard scoped per session is tested by ending a session, not by restarting the machine
2026-07-28 | Panel contacts were driven with ros2 topic pub --once all day | One start press out of dozens never reached the bridge's subscriber (--once exits on the first match, racing multiple subscribers), and the refusal was indistinguishable from a program refusal until the CSV was checked | Verify delivery in the evidence CSV before interpreting any refusal, and prefer the committed stimulus tool or repeated publishes over bare --once
2026-07-28 | An externally drafted milestone plan was adopted as a premise: fork a named forklift repo, keep its meshes, route teleop through the F-PLC | The named repo had no license, differential-drive kinematics and no meshes, and no F-CPU program exists in this project — three of the plan's five phases rested on false premises | An external plan is evidence of intent, not of the world; verify its factual premises against the repository and the sources it names before any of it enters the roadmap
2026-07-28 | m3-36's evidence CSV was archived and committed while its bridge session was still appending to the source | The committed file is a snapshot of a live 356 MB stream and the session held its PLCSIM connection for hours after "capture" | A session is ended by observation — process gone, file quiet — never by assumption; stop the session before archiving its CSV, or label the commit a live-stream snapshot
2026-07-28 | A completion agent for m3-36 and the m4r2-02 closure-line instruction were both issued from the session-start snapshot while a parallel session was landing the same ground | The work was already committed and the brief instructed a false statement; both were caught by the executing agents, not by the orchestrator | Under concurrent sessions, re-read git log and the report directory at issue time, every time; a snapshot is not a state, and "issued" is unknown until re-checked
2026-07-29 | The m4f-05b done_when was written from m4f-01b's sweep enumeration, which had matched remembered phrasings of the withdrawn steer exemption | Two bridge-design rows restated the exemption in their own words and survived the fix; the list that read as exhaustive was a report's, and it propagated into a brief's exit criterion | Sweep by subject — every occurrence of the node, tag or value name, whitespace-normalised, each hit read for dependency — never by remembered phrasing; and never let a prior sweep's enumeration bound a later brief's done_when
2026-07-29 | SPEC §11 step 5.4.4 released the monitored reset while the zone was still occupied, then 5.4.7 re-asserted it after the zone cleared and expected the latch to hold | An assertion after a release is a fresh rising edge with the cause already gone, so a correct program clears the latch and the step measures nothing; an owner running it would have hunted a defect that does not exist, and 5.4.9 repeated the shape on the enable | A test of an edge-triggered control holds the control unbroken across the event under test; a release inside the procedure is itself a stimulus, and every re-assert must be named as the edge it is
2026-07-29 | The forklift program's test procedure was reviewed as prose and looked sound | Two phantom-failure steps survived review and were found only by transliterating SPEC §7 into an executable double and running the procedure against it | Build the executable stand-in before the owner types the program into the tool: a spec defect found by a double costs an edit, the same defect found at a watch table costs a debugging session against correct logic
2026-07-29 | m4f-07's first teleop-loop harness waited synchronously inside the loop that pumped its own OPC UA session | The wait stalled the session's keep-alives, so an observed link drop could not be attributed to the code under test | A harness never blocks the event loop of the session it observes; wait by deadline-polling in a second task, and treat any harness-side stall as invalidating the observation window
2026-07-29 | The obstacle evaluator's affirmative validity counted only finite in-window samples, so a forward sector entirely beyond lidar range (all inf) read as "no valid data" and the fail-safe latched a process stop in open space | The live teleop run stopped whenever the heading opened up, with a healthy 10 Hz scan behind the false verdict; the model evidence had even recorded "all inf => TRUE at 0.000" as a bounding case without questioning its semantics | A rangefinder's beyond-range return is a measurement (clear to range_max), not missing data: validity needs three classes — clear-beyond-range, in-range distance, invalid — and a fail-safe proven against sensor death must also be proven against an empty horizon
2026-07-30 | FB_ForkliftTeleop_DB was renamed to ForkliftControl_DB while the DemoCell server interface was bound to it | The rename dragged all 19 interface local-data references onto the new name in one stroke, and the manual repair introduced a silent BridgeHeartbeat_1 browse name that cut the bridge with no error dialog — the counter froze and the inputs reverted before anyone saw a warning | No DB is renamed after a server interface binds it; the name is written correctly the first time. And after every download the browse names are swept for TIA's silent "_1" collision suffixes — it appends them without asking, in DB statics and interface rows both
2026-07-30 | The public README milestone table was hand-edited while roadmap.md held the ADR 0008 order | The table forked two gates from the authoritative roadmap and silently dropped two rows, so the next session inherited a corrupted baseline | A plan change enters the repo as an ADR round; roadmap.md is the single source for gate numbering and every other table follows it via a brief
2026-07-30 | The ADR 0010 inventory read CLAUDE.md §6 expecting the ADR 0008 order | The contract's own gate table was three renumber rounds stale — every prior round swept outward from roadmap.md and no round's brief list included CLAUDE.md §6 | A renumber round starts from a fresh whole-repo inventory that explicitly includes the contract file, never from the previous round's file list
2026-07-30 | TODO's owner build item was deleted when the CPU build completed | The item carried a half-closed sub-item (ResetEdgeMemory_1 vs SPEC §3.2) that vanished with it, leaving a live program/spec mismatch untracked | A parent item is deleted only after every sub-item is individually closed or re-homed; deleting a parent is not closure of its children
2026-07-30 | The m5r-06 brief enumerated "fixed-cell SF-08 M9 -> M6" and the agent complied | ADR 0010 D7 sends only SF-05/06 to M6 and the SRS holds SF-08 at M5, so the brief manufactured an unauthorised SF landing and the sweep propagated it | A brief's mapping enumeration is derived from the ADR at writing time and checked against it before dispatch; the verifier checks the mapping itself, not only whether the sweep applied it
2026-07-30 | Invariant 11 was enforced against the CLAUDE.md section 3 topology through four gates | The diagram never drew bridge/, so the HMI to PLC to bridge to simulation path that every M3 and M4 demonstration used was outside the diagram the invariant reads against, and three ADRs cited an edge the contract does not show | A layer added by ADR is drawn in the section 3 topology in the same round, because an invariant that names a diagram can only bind what the diagram contains
2026-07-30 | Four agents were dispatched and the orchestrator waited on their completion notifications | The container was suspended while idle; all four died silently at 120-byte transcripts, two having finished their file edits but none having written a report, and the wait cost eight hours | A dispatch is not a guarantee of completion: check agent liveness by transcript mtime and file activity when a wave runs long, and commit a coherent orphaned deliverable rather than risk a second reclaim taking it
2026-07-30 | A visual-capture agent and a verification agent were dispatched onto sim/ at the same time, scoped to different subdirectories | Both ran Gazebo with a GUI on the same four cores for 25 minutes and one landed three commits under the other's feet; the between-session RTF spread came out ~0.08, wider than the 0.025 beam cost it was supposed to bracket | Two agents that both RUN the simulator contend for the machine even when their file scopes are disjoint; serialise measurement work, and take performance deltas only within a single session
2026-07-30 | ADR 0011 D2 named a fallback for the unproven F-I/O path and asserted it changed no gate criterion | An adversarial judge found the fallback cannot satisfy M5 criterion (a) at all, because watch-table Modify means a human types the value and the scanner's signal reaches nothing; the ADR's own conditionality had also been dropped from four downstream documents, surviving only in the roadmap | A named fallback is tested against the gate criterion in the same breath as it is named, and a conditional decision propagates with its condition attached — the indicative mood is a claim
2026-07-31 | The mc-02 brief instructed the ADR to mark mc-01's quoted latency figures as container measurements | Neither was: both came from bridge/EVIDENCE_LATENCY.md sections whose own environment block records a WSL2 bridge host against Windows-side PLCSIM Advanced, and the container figures in that file belong to a different section the report never quoted | An environment qualifier is read from the evidence file's own environment section at the moment of quoting, never inherited from the brief that asked for the quote
2026-07-31 | The mode-arbitration spec's disagreement term was written in its obvious live form, NOT (applied <> in force) | That term is FALSE for the whole window in which the vehicle is adopting the mode it was just given, so it disarmed the enable one call after entry and made autonomous mode permanently unreachable; found by a throwaway executable double, not by review | A term comparing a commanded state with a reported one is written against the ADOPT WINDOW, not against the steady state, and is debounced by a timer whose PT is bounded by the responder's own adopt-and-report time
2026-08-04 | The "Category 3 is claimed" re-verb was swept in PL-SCENARIOS, the file the phrasing was found in | The identical live phrasing survived in TWIN-DEMO-MAP §2 and was caught only by the recovery agent's independent sweep | A re-verbing sweep is bounded by its SUBJECT across the corpus, never by the file the phrasing was first found in — the same rule as the tag sweep, applied to prose
2026-08-04 | register_map.py carried an unused robust line fit, and wiring it in as written was considered | Its trim was seeded by least squares, which is dragged onto the contaminating rack faces, so the trim converged onto the wrong surface and reported rms 0.0295 m against the wrong wall - a tight fit to the wrong thing reads as success | A robust estimator seeded by a non-robust one inherits the contamination it exists to reject; seed with a high-breakdown estimator (repeated-median) and treat a suspiciously small residual against a contaminated extraction as an alarm, not a pass
2026-08-04 | m5-07e bounded the estimator's dwell cost at "at most 0.33 deg for a dwell beginning at the stop" from one stop, one posture, one seed | The AMCL dwell measured +0.5389 deg from the EKF over the same regime and a second stop in the same run cost +0.548 deg, so the bound was exceeded 1.6x; its flat half (0.000 deg after settling) reproduced exactly | A bound derived from a single instance of an event is a sample, not a bound: state it as the observed value with its n, and call it a bound only after the event has been repeated across the conditions that vary it
2026-08-04 | Nav2's Twist-to-tricycle conversion was checked by a harness that drove the vehicle 7 m across the apron into a rack face and reported the contact as an arc | The check returned 0.09 and 0.00 m/s against 0.30 commanded and read as a result rather than a collision; a kinematic check that does not retrace its segments cannot tell a followed arc from a blocked one | A motion check retraces each segment and carries a speed-achievement column, so a held or blocked segment is visibly held rather than silently scored
2026-08-04 | The F-I/O probe read the module's online diagnostics and the CPU diagnostic buffer to explain why QBAD stayed 1 | Both were clean - "Module exists. OK", and not one PROFINET, F-I/O or F-runtime-group entry in fifteen buffer entries - while the F-I/O DB reported QBAD = PASS_OUT = 1 for the whole session | A simulated F-driver holds a channel fail-safe without declaring a fault anywhere a diagnostic reader would look; passivation is read from the F-I/O DB's own status members, never inferred from a healthy diagnostic page
2026-08-04 | The reintegration acknowledgement was attempted by writing ACK_REI from a watch table with safety mode activated | TIA refused: "Debugging of fail-safe tags is not allowed in permanent safety mode. (2206:000002)" | Fail-safe tags cannot be stimulated from the engineering connection at all in permanent safety mode - any design that plans to drive F-data by Modify is not a design, and the stand-in must sit in standard tags by construction
2026-08-04 | The PLCSIM Advanced API write was judged from the API's own read-back, which returned True for sixty seconds | The TIA watch table read FALSE for the same tag in the same window: the API writes a process image the F-driver overwrites, so the API view and the F-program view never agreed | An API write is verified in the consumer's view, not the writer's - read the value back through the layer that will actually use it, and hold the write long enough to see the two views disagree
2026-08-06 | A rehearsal's stale-scan stimulus matched the bridge node by name, and a rename was nearly made | The stimulus fails by returning False AND LETTING THE SCAN KEEP FLOWING - so a renamed node would have turned a safety test into a no-op that still reported a result, with the field never going stale and nobody seeing why | A stimulus that cannot apply its stimulus must fail LOUD and abort the test, never return a value and let the world carry on; and any helper that finds its target by name is a rename away from silently testing nothing
2026-08-06 | Torque removal was proven by showing the vehicle did not move when commanded | A STOPPED CONTACTOR PROCESS produces the identical observation - measured, 0 of 3 commands produce motion either way - so every test in this repository that asserts stillness can now pass for the wrong reason, and only the torque-off readback separates the two | Once a component can make the plant deaf, stillness stops being evidence of anything: any run asserting that the vehicle did not move must carry a POSITIVE CONTROL in the same run, showing the same command moving it when the inhibit is absent
2026-08-06 | A motion-present observation used the MEDIAN of its samples, the robust statistic, to decide whether the vehicle was moving | The median is geometrically degenerate in an aisle: 1037 of 6538 samples taken while the vehicle was demonstrably moving read NOT MOVING, because robustness to outliers optimises in the WRONG DIRECTION when the risk is asymmetric - a safety observation must not be robust against the evidence that something IS happening. Five statistics were measured and q95 chosen | Choose a statistic by which direction its error runs, not by its reputation: where one answer is safe and the other is not, prefer the tail that fails safe, and never let "robust" stand in for "correct for this question"
2026-08-06 | A warning-field topic was published at 20 Hz so that its ABSENCE would be visible to a consumer | The property only holds if the consumer honours it: a consumer that republishes the last value at a fixed rate turns the publisher's death into a standing order to keep driving fast, which is the exact inverse of what the topic exists to say | A topic whose silence must mean danger carries that rule to its consumer or it is worthless: no message inside the consumer's window MEANS occupied, and any republishing layer between them destroys the guarantee
2026-08-06 | A repeat run wrote its witness captures under the naming the run it was COMPARED AGAINST had used | The new files overwrote three committed captures of the original run - the evidence the repeat exists to be measured against - and it was caught before commit only by noticing the collision, then restored from the index and verified with gzip -t | An evidence filename carries the RUN that produced it, not the observation it records; a repeat that reuses its predecessor's names destroys the comparison it was dispatched to make
2026-08-06 | A vehicle was repositioned between runs with gz service set_pose, and the service returned true | Without an entity id the call is a silent no-op that still reports success, so a jammed vehicle was driven for 60 s against a pose that never moved and the run measured nothing | A service that returns success is claiming the CALL was well formed, not that it did anything; verify a reposition by reading the pose back, and treat any run whose precondition was never confirmed as discarded rather than repaired
2026-08-06 | Standards research used automated PDF summarisation to pull quotations out of vendor safety manuals | TWO of the summaries returned FABRICATED quotations - text that reads like the document and is not in it - which would have entered a safety document as verified evidence; they were caught only because every quotation was re-verified against locally extracted source text | Never quote from an automated summary of a document: fetch the file, extract its text locally, and verify the quotation against that extraction. A summariser's quotation marks are a formatting convention, not a claim about the source
2026-08-06 | A deferred config edit was carried in the queue as "point bridge.yaml at the Forklift groups, one edit" | Probing the server first showed the interface publishes the forklift group and the heartbeat AND NOTHING ELSE - every cell node the committed config named returns BadNoMatch by direct address - so the edit was a REPLACEMENT rather than an addition, and the committed config would have failed at connect against the controller in force | Probe the server before editing a client config written against a different build of the program; a queue item that says "one edit" describes the author's intent at the time, not the shape of the machine when the edit finally runs
2026-08-06 | A sweep was written to check that a load-bearing qualified claim never appears in its short unqualified form | The checker's own CLAIM constant was a literal instance of the short form, so the tool that enforced the rule broke it; and the sweep only found three files reading bare after it learned to normalise adjacent Python string literals as well as blockquote and comment markers | A checker must be run against itself before it is trusted, and assembled from pieces rather than exempted; and a prose sweep normalises every wrapper the corpus uses - quote markers, comment markers and string literals - because the claim you are hunting is quoted in all three
2026-08-06 | The operator map was to render the vehicle's live pose from /amcl_pose | AMCL publishes only on a filter update, so a STANDING vehicle has no pose stream at all and a naive consumer renders a minutes-old pose as though it were live - the failure is silent and looks exactly like a working display | A consumer of an event-driven estimate renders the AGE of what it has, never the value alone; treat absence of an update as information about the estimator's regime rather than as continuity
2026-08-06 | A monitoring node was to be built with zero publishers, using rclpy's constructor flags that disable the parameter services | The flags alone do NOT reach publishers-zero on Jazzy: /parameter_events survives them, so a node advertised as having no publisher would have had one, and the read-only claim would have been false in exactly the check meant to prove it - one explicit destroy_publisher is required and was found by probing rather than by reading | Prove a zero-endpoint claim with ros2 node info on the running node, never from the constructor arguments that were supposed to produce it; a framework's opt-out flags are a request, and what the graph advertises is the fact
2026-08-06 | A field-evaluation node ran its watchdog tick on the simulation clock, which is the natural clock for a node reasoning about simulated sensor data | /clock arrives over the same ros_gz_bridge that carries the scanners, so stopping the scans also stopped the watchdog meant to detect that they had stopped: the node wedged silently three times with frozen CPU time. A sibling node survived the identical event, which is what attributed the fault to the node rather than to the transport | A watchdog must not run on a clock supplied by the thing it watches; use a steady clock for any timeout whose purpose is to detect the failure of its own data source, and when one node wedges where a sibling survives, suspect the node before the stack
2026-08-06 | A rear self-return clip band was specified as -72.3 degrees, rounded from a measured -72.26 | The rounding went the wrong way and left two self-return indices inside the field, which would have held the verdict at INTRUSION for ever - a boundary derived from a measurement and then rounded for readability, in the direction that includes what it exists to exclude | Round a geometric boundary in the direction that EXCLUDES, never for readability, and prefer the measured index set over the angle that names it; a self-return inside a protective field is a permanent stop, not a marginal error
2026-08-05 | A plant defect was removed and the two committed reverse cases were expected to improve | Case B got 20x WORSE, tracking rms 0.0010 to 0.0206 m, because a 2 m straight reverse from an aligned pose is a route where doing nothing is correct - a dead actuator had been flattering it, and giving the axis authority let an unstable reverse pursuit loop act | A fix can un-mask a defect as easily as remove one: when an actuator's authority is restored, re-measure the cases where NOT acting was accidentally the right trajectory, because their committed figures are the ones that will get worse
2026-08-05 | The goal subcommand of a committed harness was invoked to re-drive four cases and raised NameError before sending anything | Its import had been lost in a refactor many sections earlier; every section since used the sibling stage path, which kept its own copy at its own call site, so a dead code path sat in a committed tool with no test and no user | An import belongs at the site that uses the name, and a subcommand no recent evidence section exercises is untested code however committed it looks - re-run it before a brief depends on it
2026-08-05 | An old-plant A/B was launched twice because the launching shell reported a path error that made the first launch look dead | The process guard correctly refused the second, but the second's output redirection truncated the first's log while the first was still writing to it - the open-file hazard of 2026-07-28 arriving through a redirect rather than a gzip | A guard that protects the machine does not protect the log: give every launch attempt its own log name, and treat a run whose log was truncated as discarded rather than repaired
2026-08-05 | A data block's absence from the OPC UA server was proved by a browse sweep finding it nowhere in the address space | Unbrowsable is not unaddressable: the old NodeId for the same block STILL RESOLVES by direct addressing, so the sweep had proved something weaker than it claimed - though probing the forbidden blocks by direct NodeId returned BadNodeIdUnknown and made the real claim stronger than the browse had shown | Prove a node's absence by addressing it directly and getting BadNodeIdUnknown, not by failing to find it in a browse; a browse sweep proves it is not advertised, which is a different and weaker claim than that it cannot be reached
2026-08-05 | An in-force timer PT of T#0MS was read as saying nothing, since the timer's IN was FALSE | It says more than that: reading IN, PT, ET and Q of EVERY timer in both blocks showed that timers which have run keep their specified PT even with IN FALSE, so T#0MS specifically means never yet run on this build - the exact inverse of the stale-instance trap, where a wrong PT persists precisely because the instance HAS run | Read IN, PT, ET and Q together and across every timer in the block: T#0MS with IN false means never yet run on this build, while a persisting PT with IN false means it has run, and the two diagnoses point in opposite directions
2026-08-05 | Two TRUE start values were typed into a new DB, retyped after they did not commit, and downloaded again | The server still read False: the DBs already existed on the CPU, so the download preserved their ACTUAL values and the start values never applied - a PLCSIM STOP to RUN was what took them, nothing being Retain | A start value governs a DB that does not exist yet; once the DB is on the CPU only a restart or an explicit re-initialisation applies one, so verify start values by reading them back after a restart rather than after a download
2026-08-05 | A verification script printed the specified start value beside the live value and the step was scored from its PASS line | It does not COMPARE the two columns, so it reported PASS while both process-stop nodes read False against a specified TRUE - the mismatch was on screen and the verdict ignored it | A checker that prints two values without comparing them is a display, not a check; either make it fail on the mismatch or write the step to say "compare that column by eye", and never let a PASS line stand for a comparison nobody made
2026-08-05 | A thirteen-pin re-point was reported done at its own step and the build moved on | One of the thirteen had not taken - network 15's reset pin still read the raw channel - and it was caught only by the text search two steps later, which is exactly what that search exists for; the missed pin was one a dead writer would have frozen in the PERMISSIVE direction | Verify a multi-site re-point by searching for what must no longer be there, not by re-reading what should now be there: a search returns zero or it does not, while a re-read of thirteen sites is where one gets missed
2026-08-05 | An in-force timer PT read T#0MS online and it matched the stale-PT trap this project already has a lesson for | It was not that trap: the timer's IN was FALSE, and an IEC timer reports PT as zero while it is not running, so the reading said nothing about the value in force | Read a timer's PT only while its IN is TRUE; a zero PT on an idle timer is the instrument, not the value, and it is indistinguishable from the stale-instance defect until the timer is running
2026-08-05 | Three rounds were spent on the arrival problem at the controller and route level - staging, leg length, tolerance geometry - each derived carefully and each failing | The plant had no authority in the regime being corrected: the steer PID supplies 6000 times the error in N.m against a 400 N.m contact scrub the model file's OWN COMMENT already recorded, so nothing under 3.8 degrees executes, while the controller's corrections on a straight leg are 1 to 2.5 degrees over 8 to 9 seconds - a straight leg was open-loop in yaw, so no route-level design could have worked | Before tuning or redesigning a controller, verify the plant executes the commands that controller actually issues IN THE REGIME THAT MATTERS; and read the parameter file's own comments first, because the number that explains the failure was already written in the file being edited
2026-08-05 | The signed cross-track rate was about to be read as a steering bias | Six runs negative and four positive, a mean 0.71 standard errors from zero against a mean magnitude of 0.084 m/m - no bias of any kind could produce that, which turned the question from "what pushes it off" into "why does the correction not bring it back" and led straight to the plant | Establish the SIGN of a systematic-looking error before hunting its source: a consistent sign is a bias and a random sign with a large magnitude is a missing correction, and the two searches have almost nothing in common
2026-08-05 | The final approach leg was lengthened from 3.0 to 4.5 m to give the vehicle more room to converge its heading, the distance derived from the worst measured entry error | It made the distribution worse - 3 of 5 clean fell to 2 of 5 - because the error is not inherited from the leg's start and then corrected, it is GENERATED ALONG the leg at roughly 0.10 m of cross-track per metre, so 50 % more leg bought 50 % more error; the new instrumented column proved it outright, the worst staging heading arriving clean and the best missing by 17 degrees | Before providing more room for an error, measure whether it is inherited or generated: an error that accumulates per metre is amplified by every metre you add, and the capacity argument that sounds obvious is exactly backwards
2026-08-05 | A vehicle held 0.015 m/s at near-full lock with ground truth frozen for 20 s, and the first hypothesis was that the plant could not execute the command | It was a deadlock between two of our own components: the closed-loop smoother's from-rest output is min(0.025, 0.0238/curvature), which at that steer angle gives 0.0136 m/s, which sits BELOW the converter's 0.02 m/s creep deadband - so the converter zeroed traction, the plant never moved, the estimator measured zero, and the smoother stayed pinned forever. Neither component owned the minimum speed and neither was wrong alone | Where two components each impose a speed floor, derive the admissible window between them and state it in one place; a floor that is higher than what the layer above can produce is a deadlock, not a safety margin, and it looks exactly like a plant that has stopped responding
2026-08-05 | "SUCCEEDED in 13.40 s" was carried as the m5-10 straight-route result, and its later failure was read first as a regression and then as a platform contradiction | It was neither: five repeats on the new machine gave 1 clean traverse, 2 recoveries and 2 timeouts, and the ORIGINAL evidence file already recorded 1 success in 4 at the same parameters - the committed figure was always one draw from a distribution straddling the acceptance criterion, and two sessions were spent hunting a cause for a difference that was sampling | When a criterion-relevant run is one of N attempts, the pass RATE is the measurement and the passing run is an illustration; quote the rate, and before diagnosing a difference between two environments, check what the first environment's own pass rate was
2026-08-05 | A goal checker required xy_goal_tolerance 0.25 m and yaw_goal_tolerance 0.15 rad at the same tick, each of which the vehicle satisfied for tens of seconds | It never satisfied both together in any failing run - 55.9 s inside the position circle, 47.1 s inside the heading window, 0 samples inside both - because a steered vehicle pays 2.1-2.6 m of travel per radian in the endgame, so correcting one yaw tolerance costs ~0.32-0.39 m against a 0.25 m box | A conjunctive arrival test on a vehicle that trades position for heading is unsatisfiable unless xy_tol > R x yaw_tol with R the measured metres-per-radian; check that product before tuning either term, and prefer controlling the arrival heading upstream, because a wider checker hides the geometry rather than fixing it
2026-08-05 | A package was judged safe to remove by reading apt-cache rdepends, which listed four Mesa packages as dependants | The cache lists dependants of the OLDER versions, so the reverse-dependency view described a machine that was about to stop existing; dpkg's installed control fields told the true story, and the real mechanism was a package split rather than an orphan drop - the new driver ships its own private copy | Read dpkg's installed control fields, not apt-cache rdepends, when deciding whether a removal is safe, and settle it with a live check: a process that prints the renderer you care about either maps the library or does not
2026-08-05 | ros2 lifecycle get reported "Node not found" for a node that was active, in both runs and always on the first query after a fresh CLI daemon | Cross-read through the node's own get_state service it was active every time, so the CLI's cold daemon cache was being read as node state - a verification that would have failed the run for a defect that did not exist | A cold ros2 CLI daemon cache is not a node state; cross-read a lifecycle verdict through the node's own service, and stop the daemon in teardown so the next run cannot inherit the cache
2026-08-05 | m5-21 realigned four Fast-DDS packages to fix an ABI collision, and the fix was understood as those four having been stale | They had not moved at all - they were already the archive's current builds, which is precisely why they collided; what was stale was the other half of the pair, rosidl-typesupport-fastrtps, five months behind | An ABI collision names two halves and the stale one is not always the half you upgraded; identify which side actually moved before recording the cause, or the post-mortem teaches the wrong lesson
2026-08-05 | A failed Nav2 route was written up as a regression caused by the same day's package install, and the judge brief was written to ask what the "regression" cost | EVIDENCE_NAV2 §0's own environment block says every committed figure is a 4-core container run and that nothing in it had been reproduced on the WSL showcase machine, so the run was the first attempt on that platform, not a second; the Nav2 version was identical on both sides, killing the package hypothesis outright. The orchestrator made the LESSONS-2026-07-31 mistake while quoting that same rule to the agent it was briefing | Read the evidence file's own environment block before writing the word "regression"; a comparison across two environments is a first attempt, and citing a rule in a brief is not the same as applying it to the brief
2026-08-05 | Two IncludeLaunchDescription calls in one composition each declared params_file with its own default, and the composition was read as giving each include its own value | IncludeLaunchDescription does not scope launch configurations and DeclareLaunchArgument skips its default when the configuration is already set, so the second include inherited the first's file and Nav2 silently ran on amcl.yaml - NavfnPlanner and base_link, with no error in any log | A launch configuration set by one include is visible to the next; wrap each include in GroupAction(scoped=True), and treat "the planner is not the one I configured" as a scoping bug before it is a tuning bug
2026-08-05 | model.sdf fixed its gz topic names deliberately, documented as "so they survive the model being spawned under another name" | The fixing is correct at one vehicle and is a shared-topic collision at two, so the robustness argument held only for the instance count nobody had questioned; found by reading the contract, not by running it | A name fixed so it survives being renamed is a single-instance assumption wearing a robustness costume; a contract written at n=1 is re-read at n=2 before any gate multiplies it
2026-08-05 | fastcdr alone was upgraded to clear an undefined-symbol death in nav2_amcl and controller_server, ldd having reported nothing missing | 2.2.5 to 2.2.7 keeps libfastcdr.so.2 but is not drop-in, so every ROS 2 process on the machine broke, the Gazebo bridge and the EKF included; restoring the saved 2.2.5 file restored the machine and confirmed the attribution | Fast-DDS moves as a coherent set - fastcdr, fastrtps and both rmw_fastrtps together - and a preserved soname is not an ABI promise; save the file you replace so the attribution is testable in one step
2026-08-05 | The m5-11 pass-through observation was committed with its residual and its latency as one result | Re-run four times on the installed stack, the residual reproduced exactly (0.000e+00, every pair, 221/224/676/440) while the latency came out up to 60x and 71x the committed mean and max - the run on the OLD stack matched and the clean run was the worst, so it was never a packaging effect | A single observation can hold a design property and a timing sample at once; state which half is a property of the design and which is one draw, because a re-run will reproduce the first and will not reproduce the second
2026-08-04 | The envelope gate released its actuator topics at the last step of its stop ramp, on the reasoning that the ramp had reached rest | The last value it emitted was 0.0250 m/s, and the downstream converter republishes a held command at a fixed rate, so the vehicle crept 0.0852 m in 3.3 s and would not have stopped | A state whose purpose is to stop publishing publishes its terminal value first and only then falls silent; releasing one message early is releasing at the wrong value
2026-08-04 | "Go silent" was treated as a way to hand control back, with the absence of messages read as the absence of a command | A downstream node that republishes the last command at a fixed rate turns silence into a standing order, so ceasing to publish commands the last value forever rather than nothing | Where any consumer republishes, silence is a command and not an absence; a handover is an explicit value, and the republish behaviour of every downstream node is read before designing one
2026-08-04 | The independent witness for an API write was assumed to be the engineering tool, and TIA's GUI was automated to reach a watch table | The clicks never reached the project tree and three attempts were spent on it, while the CPU's own OPC UA server was live the whole time, served a mirror of the F-program's outputs, and did not expose the written DB at all — which makes it a strictly better witness than the tool, because it cannot echo the writer | Look for a second witness on a different protocol stack before automating a GUI; the strongest witness is one that cannot see the datum you wrote, only its consequence
2026-08-04 | ADR 0011 D2's fallback was named as "the path the project already runs", inert by construction | Its stated mechanism is watch-table Modify on fail-safe-adjacent data, and the probe found TIA refuses fail-safe Modify outright in permanent safety mode (2206:000002), so the fallback could not have run as written and its inertness was never tested | A fallback described as already-running is verified to still run under the conditions the primary path would have imposed; "we already do this" is a claim about a different configuration until it is exercised in the new one
2026-08-04 | Agent waves of three and four were dispatched in parallel through this session | One container suspension killed four at once with two of them mid-deliverable and none having written a report, and three further runs were lost to usage limits; the parallel shape both reached the limits sooner and multiplied each loss | Run one agent at a time unless the work is genuinely independent AND short, and require every brief to write intermediate results into the evidence as they land rather than holding them for the end
