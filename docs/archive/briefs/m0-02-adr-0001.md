gate:                M0
agent:               infra
goal:                ADR 0001 records the thirteen section 2 architecture invariants as an accepted decision.
invariants_touched:  none (recording them, not changing them)
inputs:              [CLAUDE.md sections 2, 3, 8]
deliverable:         docs/adr/0001-architecture-invariants.md
done_when:           The file uses the section 8 ADR format (Status, Context, Decision, Consequences, Alternatives), Status is accepted, and the Decision section lists all thirteen invariants with their meaning preserved exactly; Alternatives records at least the rejected options already named in CLAUDE.md (e.g. MuJoCo, custom fleet schema, PLC as OPC UA client).
forbidden:           [altering or reinterpreting any invariant, writing application code, editing files other than the deliverable and the report, adding dependencies, committing to git]
